# Context — T103

Obiettivo: esportare le traduzioni Lovable in JSON consumabili dall'app Windows 11 (FolderTextMerger).

Vincoli:
- Lovable è source of truth.
- L'app attualmente usa JSON locali (IT/EN) e gestisce lingue.
- Serve un formato JSON stabile (schema) e una procedura di export ripetibile.

Deliverable:
- Specifica formato JSON
- Procedura export (manuale o script) documentata
- Output JSON pronti da importare nel progetto app
